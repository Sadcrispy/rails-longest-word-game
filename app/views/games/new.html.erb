<h1>🇵🇱 NIU GEJM 🇵🇱</h1>
<br>
<ul class="">
  <% @letters.each do |letter| %>
    <div class="m-3 p-2">
      <li><%= letter %></li>
    </div>
  <% end %>
<br>
<form class="flex" action="/score" method="POST" data-turbo="false">
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= hidden_field_tag :letters, @letters %>
  <input type="text" name="word" placeholder="you knoe what to doe">
  <input type="submit" value="🇵🇱">
</form>
</ul>
